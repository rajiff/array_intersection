image: openjdk:11-jdk-slim

stages:
    - build
    - test

build_java:
    stage: build
    script:
        - ls -l
        - javac ArrayIntersectionFinder.java

test_java:
    stage: test
    script:
        - ls -l
        - javac ArrayIntersectionFinder.java
        - java ArrayIntersectionFinder
